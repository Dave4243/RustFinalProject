mod model;

use crate::model::{Layer, Sigmoid, Network};

// use model;

fn main() {
    // Sigmoid(1.0, 1.0, 2.0);

    let file_name = "";

    let mut network = Network::new();

    let layer = Layer::new();

    network.add_layer(layer);    

    // println!("Hello, world!");
}
